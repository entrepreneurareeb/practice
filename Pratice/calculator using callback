function calculate(a,b,callback){
    return callback(a,b);{
}
}
const addition=(a,b)=>a+b;
 let subtract=(a,b)=>a-b;
   

let divide = (a,b)=>a/b
let multiply=(a,b)=>a*b;
let r=calculate(656,6,addition)
console.log(r)
